# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
# provider binary cache
.terraform/providers/
# Lambda build artifact (regenerate with: zip -j handler.zip handler.py)
handler.zip
# Terraform plan artifact
tfplan
# macOS + stray editor/session artifacts
.DS_Store
# tool runtime state (OpenCode CLI)
.opencode/
